var defaultConfig = {
  "options": {
    "paperFormat": "Legal",
    "orientation": "portrait",
    "format" : "pdf",
    "margin": "15mm",
    "timeout" : 4000,
    // "width" : "1600",        // Changes the width size. Defaults to 1280
    // "height" : "1000"
  },
  "paths": {}
}

module.exports = defaultConfig;
